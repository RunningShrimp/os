# NOS Kernel 破坏式重构计划

## 执行时间: 2025-12-29
## 当前错误数: 2894
## 目标: 减少到 < 500 错误

---

## 🔴 关键问题识别

### 问题 1: 基础类型导出系统崩溃 (优先级: P0)
**影响**: 1781 个 E0425 错误
**症状**: String, Box, Vec 等基础类型找不到
**根本原因**: lib.rs 重导出机制失效

### 问题 2: 顶级模块路径混乱 (优先级: P0)
**影响**: 681 个 E0433 错误
**症状**: crate::syscalls, crate::services 等顶级模块解析失败
**根本原因**: 模块声明与文件结构不匹配

### 问题 3: nos-api 类型不兼容 (优先级: P0)
**影响**: 300+ 错误
**症状**: nos_api::interfaces::* 类型找不到
**根本原因**: API crate 与 kernel 类型定义冲突

### 问题 4: 核心函数缺失 (优先级: P1)
**影响**: 200+ 错误
**症状**: init, shutdown, mmio_read32 等函数找不到
**根本原因**: 模块实现不完整

### 问题 5: 设备管理类型冲突 (优先级: P1)
**影响**: 100+ 错误
**症状**: DeviceType 等类型路径不一致
**根本原因**: 重复定义

---

## 🎯 重构策略

### 阶段 1: 修复基础类型导出 (P0)
**目标**: 解决 1500+ E0425 错误

**操作**:
1. 重写 `kernel/src/lib.rs` 的 prelude 模块
2. 创建 `kernel/src/prelude.rs` 统一导出所有基础类型
3. 在所有模块中使用 `use crate::prelude::*;`

### 阶段 2: 重组模块结构 (P0)
**目标**: 解决 600+ E0433 错误

**操作**:
1. 审查所有顶级模块声明
2. 移除重复/冲突的模块路径
3. 统一使用 `subsystems::*` 路径
4. 删除错误的顶级模块别名

### 阶段 3: 创建类型适配层 (P0)
**目标**: 解决 300+ API 类型错误

**操作**:
1. 创建 `kernel/src/api/adapter.rs` 类型适配器
2. 为所有 nos_api 类型创建本地别名
3. 统一错误类型系统

### 阶段 4: 补全核心函数 (P1)
**目标**: 解决 200+ 函数缺失错误

**操作**:
1. 为缺失的 init/shutdown 函数创建 stub
2. 实现 mmio_read32/mmio_write32 硬件抽象
3. 创建统一的初始化框架

### 阶段 5: 统一设备管理类型 (P1)
**目标**: 解决 100+ 设备类型错误

**操作**:
1. 删除重复的设备类型定义
2. 创建单一 `kernel/src/platform/device.rs`
3. 统一所有设备类型导出

---

## 🚀 执行计划

### 并行 Agent 分配

#### Agent 1-2: 修复基础类型导出系统
- 创建 prelude.rs
- 重写 lib.rs 导出
- 批量更新导入

#### Agent 3-4: 重组模块结构
- 修复顶级模块声明
- 移除冲突路径
- 统一子系统导入

#### Agent 5: 创建类型适配层
- 实现 API 适配器
- 统一错误类型
- 创建类型别名

#### Agent 6: 补全核心函数
- 实现 init/shutdown
- 添加 MMIO 函数
- 创建硬件抽象

#### Agent 7: 统一设备类型
- 删除重复定义
- 创建统一设备模块
- 更新所有导入

---

## 📊 预期结果

- **重构前**: 2894 个错误
- **重构后目标**: < 500 个错误
- **预期减少**: 2400+ 错误 (83%)
- **关键成功指标**:
  - ✅ 基础类型可访问
  - ✅ 模块路径解析正常
  - ✅ API 类型统一
  - ✅ 核心函数完整
  - ✅ 设备类型一致

---

## ⚠️ 风险评估

1. **破坏性变更**: 中高 - 需要大量文件修改
2. **引入新错误**: 低 - 使用 stub 模式
3. **功能退化**: 极低 - 只修复编译问题
4. **时间成本**: 中 - 预计 30-40 分钟

---

## 🎯 成功标准

1. cargo check 通过率 > 80%
2. 无基础类型找不到错误
3. 模块路径解析成功率 > 90%
4. 所有 agents 完成任务
