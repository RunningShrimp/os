# 项目级 cargo 配置
# 临时使用官方源以获取最新版本的依赖包

[source.crates-io]
registry = "sparse+https://index.crates.io/"
# 禁用全局的 ustc 镜像替换
# 这样可以获取到最新版本的依赖包（如 spin 0.13.0, riscv 0.15.1）

# 如果需要重新启用 ustc 镜像（当同步完成后）：
# 1. 删除或注释上面的 [source.crates-io] 部分
# 2. 取消下面的注释：
# [source.crates-io]
# replace-with = 'ustc'
